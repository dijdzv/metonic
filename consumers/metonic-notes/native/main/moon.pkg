import {
  "local/metonic_notes/production" @model,
  "local/native_host/application" @host,
  "local/native_host/window_host",
  "moonbitlang/async",
  "local/metonic_notes_native/storage",
  "moonbitlang/core/env",
}

pkgtype(kind: "executable")

supported_targets = "native"

options("link": {"native": {"cc-link-flags": "/link /SUBSYSTEM:WINDOWS /ENTRY:mainCRTStartup"}})
