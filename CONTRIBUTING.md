# 開発と運用

初期は一人でも滞らない、main中心の運用にする。

## ブランチとPR

- `main` は統合先。通常の変更は短命ブランチとPRを使う。
- ブランチ名は `feat/<issue>-<topic>`、`fix/...`、`spike/...`、`chore/...`、`docs/...`。
  初期構築や小修正はIssue番号を省略してよい。
- 1 PRは1つの判断・検証・実装単位。途中ならDraft PRを使う。
- PRには問題、変更後の動作、検証結果、未実施事項を書く。関連Issueをリンクする。
- 完了条件をすべて満たす場合だけ `Closes #N` を使う。部分実装は `Refs #N`。
- squash mergeを基本とし、マージ後に作業ブランチを削除する。
- mainのforce push・削除は禁止。作業ブランチの履歴整理は共有者と衝突しない範囲で行う。
- 一人運用では他者のapproveを必須にしない。差分確認と対象テストをマージ条件にする。
- CIが整った段階で安定したチェックだけを必須にする。未実施のGUI試験をCI成功と同一視しない。

GitHub側もmainへのPR必須、approve人数0、会話解決必須、線形履歴、
force push/削除禁止、管理者にも適用を設定済み。現段階のrequired status checksは未設定。

## Issue

- タスクメモ、調査、バグ、設計判断に気軽に使う。小さな修正はPRだけでもよい。
- 最低限「目的」「完了条件」を書く。未確定事項と依存Issueは必要時に追加する。
- ラベルは `type:task` / `type:spike` / `type:bug`、`area:tooling` / `area:ui` /
  `area:rpc`、`priority:p0` を初期セットとする。必要になった時点で増やす。
- P0の各検証を個別Issueにし、進捗はIssue本文・PR・検証記録で追う。
- 実装済みと検証済みを区別し、未達の完了条件が残っているIssueは閉じない。

## リリース

- 通常はmain上の確認済みコミットに `v0.x.y` タグを付ける。常設develop/releaseブランチは作らない。
- 最初の試験配布は `v0.1.0-alpha.1` 等。設計書v0.2は製品バージョンではない。
- 過去版を並行保守する必要が生じたときだけ `release/0.x` を作る。
- タグは移動しない。修正は新しいタグで配布する。
- 配布物ができてからCDを追加する。署名・ストア公開・パッケージ公開は配布先が決まってから。

## ローカル検証

セットアップは docs/development.md を参照。
`mise run verify` がフォーマット・型付き契約試験・コンパイル失敗試験の入口。
Windows SDKとMSVCがある環境では `mise run verify-native` も実行する。

## ライセンス

新規成果物は `MIT OR Apache-2.0`。利用者はいずれかを選べる。
貢献も同じ条件とする。第三者コード・フォントのライセンスは別途保持・記録する。
