# Agent rules

- 設計は DESIGN.md と docs/adr/、作業手順とGit運用は docs/development.md を参照する。
  個別ライブラリの候補・評価状況・次のタスクは設計資料・検証資料・Issueで管理し、ここに重複させない。
- コードに **How**、テストに **What**、コミットログに **Why**、コードコメントに **Why Not** を残す。
  テストは期待する振る舞い・契約を示す。コメントは自明な処理説明ではなく、
  一見自然な別の方法を採らない理由や、変更時に見落としやすい制約を記す。
  squash後にも変更理由が残るコミット本文にする。
- 再現手順・測定結果・設計判断などの技術記録は docs/ に置いてよい。
  実装済み・検証済み・未実施を区別して更新し、コード・テスト・履歴の説明を重複させない。
- 個人的な会話ログやエージェント間の引き継ぎメモはコミットしない。
- mainはPR経由で更新する。force push・ブランチ削除・PR必須設定の解除を行わない。
- 整形・静的検査・テストはpre-commitで実行し、成功前にpushしない。
  PR/push CIでローカルと同じ検証を重複実行しない。
  ローカルで網羅できない範囲が生じたときに、目的を明確にして追加のCI検証を導入する。
- miseタスクを使い、toolchain.jsonに固定した現行MoonBitで検証する。
- 開発スクリプト・検証・CLIもMoonBitを基本とし、単独スクリプトには`.mbtx`を使う。
  単に既存スクリプトを呼ぶだけのラッパーを、MoonBitへの移行完了としない。
  OSの初期設定や外部JS APIとの接続など、別言語が必要な境界は最小限に限定し、
  必要性と残る範囲を記録する。便利さだけを理由に別言語の処理を増やさない。
- ブリッジ等を自作する前に既存ライブラリを比較し、有効性を検証した部分から再利用する。
- Computer Useによる画面操作は使わない。UI検証はCLI/MCPとヘッドレス実行を優先し、
  操作・状態取得・描画結果確認を再現可能な自動検証として実装する。
- nativeのヘッドレス状態/オフスクリーン描画検証と、実ウィンドウ・IME・OS
  アクセシビリティの統合検証は区別する。後者を前者の成功で代替しない。

## モデルと責務

- メインエージェントは GPT-6 Astra (`gpt-6-astra`) を使う。
- 設計、調査、計画、仕様判断、作業分割、レビュー、検証、統合はメインが担当する。
  この運用規約や設計資料の作成・更新もメインの担当とする。
- 合意した設計に沿う実装（テストコードの作成・修正を含む）は、GPT-5.6 Luna
  (`gpt-5.6-luna`) のサブエージェントへ委譲する。メインが実装を代行しない。
- コマンド実行はメインだけが行う。ビルド、テスト、フォーマッター、生成処理、
  依存導入、Git操作、CI確認、PR作成・マージもすべてメインの責務とする。
- Lunaは指定範囲のファイル参照・編集と変更内容の報告に限定する。
  シェルコマンドやテストを実行せず、独自の設計変更・追加委譲を行わない。
- 指定モデルを利用できない場合は、その制約を報告する。別モデルを同一扱いで黙って代用しない。

## 実装の委譲とファイル所有権

メインは委譲前に、次の内容をサブエージェントへ明示する。

1. 実装する目的、確定した設計、受け入れ条件。
2. **編集・作成を許可するファイルの明示的な一覧**。ディレクトリ全体や曖昧な範囲では渡さない。
3. 必要な既存コード、インターフェース、参照資料と、変更してはいけない境界。
4. 実行禁止の操作と、実装後にメインが行う検証。

- 同時に動くサブエージェントの担当ファイルは完全に分離する。
  同じファイルの別の行を編集する場合も、並列では割り当てない。
- メインも、委譲中の担当ファイルを並行編集しない。全体フォーマットやコード生成も、
  対象ファイルの担当が戻るまで実行しない。
- 共有ファイルの変更が必要な実装は直列化する。担当変更は前のエージェントが編集を終え、
  メインが差分を確認して所有権を回収してから、次のエージェントへ明示して渡す。
- 許可一覧の外の変更が必要になった場合、Lunaはそのファイルを編集せず、理由と対象をメインへ報告する。
  メインが設計・競合を確認し、担当範囲を更新してから作業を続ける。
- Lunaの完了報告には変更ファイル、実装内容、残る疑問を含める。
  実行していないテストを成功と報告しない。
- メインは返却差分をレビューし、必要な修正を再委譲し、コマンドとテストで検証してから統合する。

委譲ごとの担当一覧は作業中のメッセージで管理し、個人的な引き継ぎ資料としてコミットしない。
