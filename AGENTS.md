# Agent rules

## 指示と記録の置き場所

同じ規約を複数の場所に転記せず、下記の正本を参照する。

| 場所 | 扱う内容 |
| --- | --- |
| このファイル | このリポジトリでのエージェント固有の行動規約 |
| [開発ガイド](docs/development.md) | 環境、コマンド、検証、Git・PR・CI・Issue運用 |
| [設計](DESIGN.md)・[ADR](docs/adr/) | 継続的な設計判断、依存選定、制約 |
| [検証資料](docs/verification/) | 再現手順、確認できた範囲と限界 |
| Issue | 個別タスクの完了条件、優先順位、依存関係、途中経過 |
| 目標 | 今回達成する成果、対象範囲、全体の完了条件。運用規約は転記しない |
| スキル | 複数の作業で再利用する具体的な手順。必要になった時だけ切り出す |

別のエージェントルールファイルは増やさない。個人的な引き継ぎ資料や
会話ログをコミットしない。単発の変更指示は実行して完了させ、
恒常的な規約や目標の中に残さない。

## 実行体制

- 当面サブエージェントを使用しない。メインが設計、調査、実装、
  フォーマット、静的解析、テスト、レビュー、Git・Issue・PR操作を担当する。
- 作業を意味のある単位で検証まで進める。同じ検証は変更・失敗・
  未解決の懸念などの根拠がある場合に再実行する。
- 実行結果と未検証事項を区別する。目標の完了はその全体の要件で判断する。
- 節目ごとに確認済みの状況、残る課題、次の作業をユーザーへ報告する。
  新しい証拠や失敗を受けて優先順位・分割・検証方法を見直し、改善があれば
  計画へ反映する。Issueの更新方法は開発ガイドに従う。

## 実装と検証の判断

- コードに **How**、テストに **What**、コミットログに **Why**、
  コードコメントに **Why Not** を残す。squash後にも変更理由が残る本文にする。
- 開発スクリプト・検証・CLIもMoonBitを基本とし、単独スクリプトは
  `.mbtx` を使う。未変更の別言語スクリプトを包むだけでは移行完了としない。
- 別言語は必要な外部API・OS境界に限定し、残す理由を記録する。
  ブリッジを自作する前に既存ライブラリを比較し、有効性を確認して採用する。
- 依存ライブラリ・ツールのバグや、その目的に照らした明確な機能不足は、
  自作や回避策を選ぶ前に上流への修正提案を検討する。最新の上流実装と
  既存の報告を確認し、最小再現・期待される契約・検証可能な差分を整理する。
  ローカルの修正や回避策が必要な場合も、その理由と上流対応の検討結果を残す。
- UIの自動検証にはCLI/MCPとヘッドレス検証を使い、実入力・表示の確認には
  Computer Useも使用する。
  証拠の範囲の扱いは開発ガイドの「Recording experiments」に従う。
