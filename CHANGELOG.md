# 設計変更履歴

## v0.2 — 2026-09-06

v0.1以降の、Web/JS資産の利用、ブラウザホスト、入力・IME、コンポーネントの品質、名称保留に関する会話を統合した改訂版。`DESIGN.md`は差分だけの追補ではなく、既存設計を含む全文である。

この版は設計文書の更新であり、フレームワークの実装・ビルド・実機試験を完了したという意味ではない。

### 今回の主な変更

| 項目 | v0.2で明確にした内容 | 設計書の主な位置 |
| --- | --- | --- |
| 名称 | 未定。Rune/Muneを含む候補は未採用。Codexは命名で作業を止めず、独断で改名しない | 冒頭、F-16、16章、17章 |
| Web資産の再利用 | 中核の完成条件にしない。具体的なライブラリが必要になった時の追加機能 | F-14、1.3節、8.4〜8.6節 |
| 互換レンダラー | React/Solid/Vue向け互換層は作らない。自分のGPUレンダラーは引き続き作る | F-13、2.5節、3.2節 |
| 優先度 | gRPCは高優先度の任意機能。JS/WebView連携は低優先度の任意機能 | 1.3節、15.3節 |
| ブラウザのビルド | WasmGC＋最小JSホストを優先検証。MoonBit→JS案も比較。ホストをMoonBit→JSで別ビルドすることは必須ではない | 1.2節、8.2節 |
| ホスト境界 | 初期化、データ変換、非同期、取消、寿命、再入、転送量、配布物を検証項目にする | 8.2.2節、P0-B/C |
| 状態の所有 | アプリ状態・リアクティブグラフをWasmとJSの両側へ重複実装しない | 8.2.1節、12.2節 |
| JSロジック | アダプターは必要な場合だけ。Windowsでの埋め込み/別プロセス等は未採用 | 8.5節 |
| 標準ランタイム | Node/Bun/WebViewをアプリの必須実行環境にしない。ビルド時ツールとは区別 | 8.1節、12.2節、A1 |
| Webパネル | Windows=WebView2候補、Browser=DOM/iframe候補。自動変換でもGPUテクスチャ化でもない | 8.6節 |
| Webパネルの範囲 | 必要時に独立した矩形領域として検討。focus・寿命・通信・権限制限を持つ。全GPU合成互換は約束しない | 8.6節、15.3節 |
| 入力とIME | WindowsはOS入力、BrowserはEditContext/入力DOM。WebPanelとは別の必須基盤 | 2.3節、9章 |
| 早期入力試験 | P0-Gを追加。EditContextの機能検出だけでなく日本語変換・選択・候補位置を試す | P0-G、I1 |
| 最小コンポーネント | 操作基盤、少数のStyled部品、共通テーマ、ギャラリー、使用例、拡張規約、操作試験を重視 | 7.4〜7.5節、C1 |
| 根拠の整理 | 新しい境界について一次資料を再確認。今回確認した範囲とv0.1から継承した資料を区別 | 18章 |

### 変更していない中核方針

主言語はMoonBit、FE/BEともMoonBitで記述する。JSXと独自UI構文は導入しない。Windowsネイティブを最優先にし、ブラウザWebGPUも最初から第一級ターゲットにする。他OSのGUI対応を初期の完了条件にはしない。

Signal/Memo/Binding/Effect/Resource、Scope寿命、持続するUIノード、工程別のdirty管理を用いる。Effectはフレームワークの副作用管理であり、言語エフェクトシステムの実装ではない。GPUIのEntity単位の更新モデルをそのまま移植するものでもない。

FEはBE側の `backend/api` から契約だけを参照する。汎用sharedは必須にせず、サーバー実装をFEへ取り込まない。型共有とwire互換性・実行時検証は区別する。

gRPCは高優先度の独立プラグインとし、UIから独立して進める。gRPCは単なるシリアライザー差し替えではなく、schema・protocol・capability・network carrierを分ける。gRPC対象ではproto正本とDTO生成を初期案とする。

### 会話中の曖昧さを補正した箇所

**「独自レンダラーを作らない」**は既存Webフレームワーク向け互換層の話であり、Scene→GPUの中核描画処理を取り下げる意味ではない。

**「Web資産を必須にしない」**はWebGPU対応やWasmGC候補を取り下げる意味ではない。ブラウザホスト、文字基盤の低レベル再利用、入力・IME、アクセシビリティは引き続き必要である。

**「WasmGC＋JS」**はWasmアプリとJSホストの構成案であり、アプリを必ず二種類にコンパイルして重複実行する要求ではない。UI状態の正本は一つにする。

**「IMEにDOM」**はブラウザ側の入力接続案であり、WindowsのGPUIがDOMで入力を実装しているからという説明ではない。EditContext/入力DOMの採否は実機試験で決める。

**「WebViewをブラウザへ」**はプラットフォーム別アダプターの設計案であり、自動変換でも、WebGPU経由でWebView全体を描く方式でもない。

**「AIがあるので作れる」**は共通操作基盤を使って合成UIやスタイルを作りやすくする方針であり、IMEやフォーカスの品質検証を不要にする意味ではない。

### 既存実装への適用手順

既存のv0.1実装を保持したうえで、依存一覧・ブラウザ起動経路・入力モデル・実装タスクを確認する。不要なNode/WebView必須依存や、未使用の互換レンダラー用コードを新規に追加しない。

設計に沿う低レベルコードと検証記録は再利用する。ブラウザホストの状態重複、IMEの責任分界、任意拡張の依存方向に違反がある場合は、範囲を限定して修正する。

P0-Gとホスト境界の試験を補い、gRPCを含む既存の優先度を維持する。名称変更や全面再生成は不要。

## v0.1 — 2026-09-06

初版。Windows優先・WebGPU第一級のMoonBit GPU UI、細粒度リアクティビティ、操作基盤とStyled層、型付きRPC、gRPCプラグイン、検証計画を整理し、Codexの開始指示を作成した。
