# Codexへの実装引き継ぎ — v0.2

更新日：2026-09-06 / 名称：未定 / 設計正本：`DESIGN.md` v0.2

このファイルを、同じディレクトリの `DESIGN.md`、`CHANGELOG.md` とセットで使う。設計の正本は `DESIGN.md`。この指示は最初の実装単位と進め方を定めるもので、全機能の一括実装を要求しない。

## 依頼

`DESIGN.md` v0.2を読み、既存リポジトリの構成・規約・実装済み範囲を確認したうえで、MoonBitのGPU UIフレームワークと独立した型付きRPC基盤を段階的に実装してください。

新規なら **P0：技術検証・選定** から始めてください。v0.1に基づくコードが既にある場合は、まず `CHANGELOG.md` と照合して差分を反映し、未完了の小さな検証・実装単位から進めてください。既存コードを全削除して作り直す必要はありません。

**正式名称は決まっていません。Rune、Muneなどの会話中の候補を採用しないでください。** 既存リポジトリ名は維持し、新規の場合も機能を表す暫定的なパス名で進めてください。命名調査・公開namespaceの確定・一括改名は今回の作業に含めません。

## 変更しない前提

| 項目 | 前提 |
| --- | --- |
| 主言語 | UI、コンポーネント、アプリ状態、BEの業務処理はMoonBit |
| 構文 | JSXを使わない。独自UI言語・専用構文・コンパイラフォークも初期は使わない |
| native | Windows最優先。他OSのGUI対応は初期完了条件にしない |
| Web | WebGPUを第一級にし、最初の検証から動かす。後付け移植にしない |
| 可視UI | 標準UIはnative/WebともGPU描画。主方式をWebViewやHTML/CSSへ置き換えない |
| 更新方式 | Signal/Memo/Binding/Effect/Resourceと持続するUIノード。中央Msgモデルは強制しない |
| Effect | フレームワークの副作用・寿命管理。言語のeffect systemを作る要求ではない |
| ブラウザホスト | 小さなJSホストを許容。WasmGC案とJS出力案を比較し、一つの初期経路を選ぶ |
| 入力・支援技術 | WindowsのOS連携と、ブラウザのEditContext/入力DOM/semantic接続を分ける。いずれも中核 |
| 低レベル再利用 | Rust/CをOS・GPU・文字等に使ってよい。個別Rustコンポーネントの大量bindingは避ける |
| コンポーネント | GPUI周辺のBase/Styled分離を参考に、操作基盤・少数の標準部品・テーマを揃える |
| API契約 | アプリの `backend/api` にターゲット非依存の契約を置き、FEはそれだけを参照する |
| shared | 雑多な汎用 `shared` パッケージを必須にしない |
| RPC | tRPC/oRPC的な型付き契約をMoonBitで提供する。TypeScript実装を必須にしない |
| gRPC | **高優先度の任意パッケージ**。調査・相互運用を先延ばしにしない |
| 外部JS/Web UI | **低優先度の任意拡張**。初期デモ・配布物に要求しない |
| 境界 | RPCはUIから独立。Resourceとの接続は別アダプターにする |

## 今回実装しないもの

React/Solid/Vue向けの互換レンダラー、DOM/CSS互換エンジン、任意npmパッケージの無修正実行、Node/Bunの標準同梱、WebViewの標準同梱、WebパネルのGPUテクスチャ化は実装しないでください。

**ここで取り下げたのは既存Webフレームワーク向けの互換層であって、本プロジェクト自身のGPUレンダラーではありません。** また、外部Web資産の優先度を下げたことを、WebGPUやWasmGC候補の取り下げと解釈しないでください。

`DESIGN.md` 8.5〜8.6節は、将来必要になったときの拡張境界の案です。WebPanel、JSランタイム、プラグインローダー、空の拡張パッケージ群を先回りで作る指示ではありません。特定の利用要件が発生するまで依存追加・公開API固定をしないでください。

ブラウザのWebGPU・入力・アクセシビリティ用のホストは上記の任意拡張とは別です。JS/WebView拡張がなくても、標準TextInputで日本語が入力できなければなりません。

## 最初の作業単位：P0

### 1. 現状とツールチェーンを記録する

既存ソース、テスト、リポジトリ規約、MoonBit/Rustの設定を確認してください。MoonBitのnative、JS、WasmGCに対する依存ライブラリの対応を調べ、バージョンまたはコミットを記録してください。

掲載APIは未実装の概念例です。存在を確認していないAPI名、FFIの挙動、型推論、設定構文を推測で使わないでください。`latest`の説明だけで動作済みとせず、小さなビルドで確認してください。

nativeではWindowsで必要なC ABIを使える経路を確認してください。ビルド時ツール、配布アセット、実行時依存を分け、Node等の開発依存をアプリ実行時の必須ランタイムと混同しないでください。

### 2. Windowsとブラウザで最小の描画経路を作る（P0-A/B）

WindowsではMoonBit nativeから低レベルブリッジを呼び、ウィンドウ、矩形、入力、resize、終了までを通してください。winit等のイベントループとMoonBitの非同期ランタイムの責任主体を明確にしてください。

nativeのGPU連携は `wgpu-native` の利用かRust `wgpu`ラッパーかを比較し、一つの初期経路に絞ってください。

ブラウザではMoonBitで書いた小さな状態・描画ロジックからWebGPUへ到達させてください。まず `wasm-gc`＋最小JSホスト案を試し、`js`出力案とも比較してください。ホストを手書きJSにするか、別のMoonBit→JSビルドにするかは実装上の選択であり、二重ビルド自体は必須ではありません。

比較対象は起動、非同期処理、バッファ転送、文字基盤、配布物です。アプリ状態・Reactive RuntimeをWasmとJSの両側に二重作成しないでください。ホスト側のGPU/DOM資源と、アプリ側の状態を分けてください。

### 3. タスクの完了・取消・寿命を検証する（P0-C）

タイマーまたは模擬非同期リクエストがUIを停止させず完了することを確認してください。Scope破棄後の結果や古いリクエストの結果を破棄してください。UI状態を他スレッドから直接更新しないでください。

ブラウザでは、JS/Wasm境界をまたぐ引数・結果・エラー・callbackの寿命を確認してください。同じMoonBit型でも、別ターゲットの実メモリ表現が自動的に共有されるとは扱わないでください。Wasm/JS間の関数呼び出しとFE/BE間のRPCを分けてください。

### 4. 契約だけをFEへ公開できることを確認する（P0-D）

一つの `Procedure[Input,Output,DomainError]` の最小モデルを作ってください。FEが `backend/api` だけを参照してビルドできること、誤ったInputやhandlerのOutputを渡すと型チェックで失敗することを確認してください。

最初の呼び出しAPIは `client.call(procedure, input)` のような明示的なものでも構いません。BE実装から任意のクライアントAPIを自動推論・生成する仕組みは後回しにしてください。契約にDB、nativeサーバー実装、秘密情報、実行用handlerを混ぜないでください。

### 5. ProtobufとgRPCのリスクを先に調べる（P0-E）

`protoc-gen-mbt` 等を固定した条件で試し、最小メッセージを既存実装と相互デコードしてください。Protobuf生成とgRPC runtime全体の完成を混同しないでください。

native gRPCの実装候補と、ブラウザのConnect/gRPC-Web経路を選んでください。サーバーアダプターかproxyかも明記してください。MoonBit型だけからProtobufのフィールド番号を自動的に復元できるとは扱わないでください。

P0で完全なgRPC製品を作る必要はありませんが、N2で何を実装すべきか判断できる証拠を残してください。UIコンポーネントやJS互換機能の完成を待つ理由にしないでください。

### 6. 文字処理の共有経路を試す（P0-F）

日本語と英数字を最小表示し、フォント取得、整形、グリフ描画、文字位置の単位を確認してください。Rustの文字エンジンを使う場合、nativeとブラウザでどう接続するかを示してください。

別のRust WasmモジュールとMoonBit WasmGCが、ヒープやRustのC ABIをそのまま共有できるとは扱わないでください。この低レベル基盤の選定は中核であり、外部Webコンポーネントの互換対応とは別です。

### 7. ブラウザのIME経路を小さく検証する（P0-G）

EditContextを優先候補として、入力用textarea等の代替経路も比較してください。APIの機能検出だけで完了にせず、日本語の変換・選択・候補位置を試してください。完全なTextInputはI1で作ればよく、ここでは境界とリスクの実証を優先してください。

Windowsの入力機構をDOMで実装する必要はありません。GPUIの入力モデルは参考にしつつ、Windowsとブラウザの接続を分けてください。

実機・IME環境がない場合は未実施項目として明示し、変換範囲・選択・文字位置の変換・イベント寿命など、実行できるモデル試験を進めてください。

## P0で残す成果物

| 成果物 | 内容 |
| --- | --- |
| 小さく実行可能な検証ソース | native/Web、async、契約、Protobuf、文字、入力経路の各検証 |
| `docs/verification/environment.md` | OS、ツールチェーン、ブラウザ、GPU、実行可能/不可能な試験 |
| `docs/verification/dependencies.md` | 固定バージョン、対応ターゲット、ライセンス確認状況、開発/実行時の依存区分 |
| `docs/verification/p0.md` | 検証コマンド、結果、ログの位置、未実行項目、次の判断材料 |
| `docs/adr/` | Browserターゲットとビルド単位、GPUブリッジ、イベントループ、文字基盤、入力経路、gRPC経路 |
| 次の小さな実装単位 | R1 / G1 / N1のうち着手可能な範囲と合格条件 |

既存リポジトリに対応する場所があれば、その構成を優先して構いません。P0を調査文書だけで終わらせず、実行できる検証ソースを残してください。ただし、実機がない試験を成功扱いにしないでください。

## P0後の順序

```text
P0
 ├─ UI：R1 Reactive Core + G1 Native/Web GPU
 │       → T1 Layout/Text → I1 Input/IME/A11y → C1 Components
 └─ RPC：N1 Typed RPC/HTTP-JSON
          → N2 gRPC plugin / Browser protocol

両系統 → A1 共通アプリへの統合・配布

必要性が生じた後だけ：E1 JSロジック / E2 WebPanel
名称決定は別作業。実装の前提条件ではない。
```

N2はN1の境界が固まり次第、C1を待たずに進めてください。入力・IME・アクセシビリティは最後の仕上げとして放置しないでください。

C1では、操作基盤とStyled層を分け、少数の部品、統一テーマ、動くギャラリー、使用例、独自コンポーネントの拡張規約、操作テストを揃えてください。AIや利用者に毎回IME・フォーカス管理から作らせる設計にはしないでください。

最初の統合目標は、**同じMoonBitアプリがWindowsとWebGPUブラウザで動き、日本語を入力し、MoonBit BEへ型安全に通信すること**です。外部JSライブラリやWebViewなしで成立させてください。

## 検証できない場合・判断が必要な場合

Windows実機や利用可能なGPUがない場合は、可能なビルド・ヘッドレス試験を進め、実機動作やIME試験を「未実施」と書いてください。未実施の試験を成功扱いにしないでください。

依存に不足が見つかったら、エラー、根拠、代替案、影響範囲を記録してください。一項目の環境不足を理由に、独立した全作業を止める必要はありません。検証済みの境界を利用して、小さな実装を進めてください。

検証不能を理由にMoonBitの主要UIを別言語/WebViewへ置き換えたり、WebGPU版を削除したりしないでください。必要な設計変更は、確定事項と設計案を区別してADRへ残してください。

## 各作業の報告形式

実装した範囲、変更した主なファイル、実行したコマンドと結果、実行できなかった試験、既知の制限、次の一単位を報告してください。性能値には測定環境とビルドモードを添えてください。

新規依存を増やした場合は、「標準の低レベル基盤」「高優先度のRPCプラグイン」「低優先度のJS/Web拡張」のどれかを明記してください。

README、interface、スタブだけを生成してフレームワーク完成とはしないでください。推測した動作より、最小でも実際に確認できた垂直スライスを優先してください。
