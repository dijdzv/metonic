[package]
name = "metonic-wgpu-probe"
version = "0.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wgpu = { version = "=30.0.1", default-features = false, features = ["dx12", "wgsl"] }
pollster = "=1.0.1"
