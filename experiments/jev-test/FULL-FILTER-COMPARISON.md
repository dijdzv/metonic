# フック全体カタログでのJev比較

対象はFULL-INVENTORY.mdの固定worktreeスナップショット。consumer単体テスト73件は
既存フック外なので含めない。利用例アプリを恒久的に残す方針を決めるものではない。

保存済みの2つのDiffについて、同じ全1,880項目をnames/symbolsでそれぞれ送った。
4回とも1,880 IDすべてに応答が返った。トークン上限による通信分割は行うが、
対象項目を事前に削らない。テスト実行・故障注入・フックの実行集合変更は行っていない。

条件は探索用score <= 0.5、confidence >= 0.6、P(0) >= 0.6。
応答欠落・不正な確率等は既存の安全側選択を維持する。閾値は今回変更していない。

| Diff | 入力 | 除外候補/1,880 | API時間 | 入力tokens | 既知の失敗の除外 |
|---|---|---:|---:|---:|---:|
| ラベル名更新を無効化 | 名前中心 | 184 | 23,412ms | 455,157 | 0/3 |
| 同上 | 名前＋シンボル | 294 | 35,927ms | 589,721 | 0/3 |
| ナビゲーションcacheのrevision照合を無効化 | 名前中心 | 477 | 23,174ms | 455,005 | 0/1 |
| 同上 | 名前＋シンボル | 735 | 36,026ms | 589,455 | 0/1 |

名前中心もfile/package/kind/IDを含む。シンボル付きはMoonBit1,237項目とNode34項目に
構文的な参照情報を加える。他の種類は現在シンボル配列が空で、情報追加はない。
ソース全文・テスト本文はこの比較では送っていない。

## 名前付きテストの内訳

シンボル付きの場合:
- ラベル名変更: MoonBit249/1,237、Node27/34が除外候補。
- ナビゲーション変更: MoonBit662/1,237、Node25/34が除外候補。

全体件数には親コマンドとその内部検証も含む。除外候補数をテスト実行削減数や
所要時間の短縮率と解釈しない。親と子の判断を集約した実行計画でもない。
既知の失敗との照合は以前保存したJS実行結果のみであり、新しい故障検証ではない。
各条件1回の比較なので、変動幅や未知の変更に対する精度は未評価。

## 判断

両Diffでシンボル付きの除外候補が増え、既知の失敗は残った。
次の比較の基準をシンボル付きとする価値がある。時間と入力量は増えるため、
フルソースへ拡張する前に、統合・契約・コーパス向けの小さな構造情報を検討する。
特にコーパス299項目は0～5候補にとどまり、名前だけでは根拠が不足している。
削減率の目標は設定しない。

生データ: .work/jev-metonic/full-comparison.json、full-{names,symbols}-{response,plan}.json、
full-navigation-{names,symbols}-{response,plan}.json。
